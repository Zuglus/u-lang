# Тест 2: Отправка строки
# Проверяет: передача String через канал

ch = Channel.new()

spawn(fn()
    ch.send("Hello from goroutine!")
end)

msg = ch.receive()
print("Test 2 - String transfer: $msg")
# Ожидается: Hello from goroutine!
