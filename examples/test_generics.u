enum Maybe[T]
    Being(value: T)
    Nothing
end

fn divide(a: Int, b: Int) -> Maybe[Int]
    if b == 0
        return Nothing
    end
    return Being(value: a / b)
end

result = divide(10, 2)
match result
    Being(v) => print("Result: $v")
    Nothing => print("Nothing")
end
