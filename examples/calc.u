// calc.u — чистые функции

fn abs(x: Int) -> Int
    if x < 0
        return 0 - x
    end
    return x
end

fn factorial(n: Int) -> Int
    if n < 2
        return 1
    end
    return n * factorial(n - 1)
end

print("abs(-42) = $(abs(-42))")
print("10! = $(factorial(10))")