// Определение процесса print через Rust-примитив println_raw из runtime.
// Это практический тест: может ли язык определять процессы в .u и
// подключать Rust-функции через use.

use u_runtime: println_raw

process print(s: String)
    println_raw(s)
end

print("hello from print.u")
